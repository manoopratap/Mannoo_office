function cyclic_rotation_of_array(params) {
    
   // let lastValue =  params[params.length-1]
   // params.splice(params.length-1 ,1)
   // console.log(parmas)
   // params.unshift(lastValue)
   // console.log(params)
var foo = function foo() {
     return 12;
 }

}

let parmas = [1,4,13,5,7]
cyclic_rotation_of_array(parmas)